// Default map center (Mumbai, India)
export const DEFAULT_MAP_CENTER = { lat: 35.227085, lng: -80.843124 };

// Map configuration
export const MAP_CONFIG = {
  defaultZoom: 15,
  options: {
    disableDefaultUI: true,
    zoomControl: true,
  },
  containerStyle: {
    width: '100%',
    height: '100%',
  },
};
