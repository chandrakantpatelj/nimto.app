// Default map center (Mumbai, India)
export const DEFAULT_MAP_CENTER = { lat: 19.076, lng: 72.8777 };

// Map configuration
export const MAP_CONFIG = {
  defaultZoom: 15,
  options: {
    disableDefaultUI: true,
    zoomControl: true,
  },
  containerStyle: {
    width: '100%',
    height: '100%',
  },
};
