export const metadata = {
  title: 'User Management',
  description: 'Manage users, roles, and permissions.',
  keywords: ['user management', 'roles', 'permissions', 'admin'],
};

export default function UserManagementLayout({ children }) {
  return children;
}
